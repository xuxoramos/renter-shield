services:
  app:
    build: ..
    restart: unless-stopped
    # Memory guard: keep the app container within 7 GB so the host OS
    # and nginx still have room on an 8 GB VPS.  The extra 1 GB swap
    # cushion (memswap_limit - mem_limit) absorbs short spikes without
    # triggering the OOM killer.
    mem_limit: 7168m
    memswap_limit: 8192m
    environment:
      - LI_OUTPUT_DIR=/app/output
      - LI_API_KEYS=${LI_API_KEYS:-changeme}
    volumes:
      # Mount scored data at runtime (alternative to baking it into the image)
      - ../output:/app/output:ro
      # Persist audit DB (user tokens/sessions) across redeploys
      - app-logs:/app/logs
    expose:
      - "8000"
      - "8501"
      - "8502"

  nginx:
    image: nginx:1-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # Uncomment after running certbot
      # - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - app

volumes:
  app-logs:
